name: bonfire
description: (RPG maker) Create RPG-style or similar games more simply with Flame.
version: 3.9.5
publish_to: 'none'
# documentation: https://bonfire-engine.github.io

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  flame: 1.17.0
  #tiledjsonreader: ^1.3.4
  tiledjsonreader:
    path: ../tiled_json_reader
  http: ^1.1.0
  a_star_algorithm: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
